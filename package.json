{
  "name": "tg",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=23.6.0"
  },
  "bin": {
    "tg": "./src/cli.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "tg": "node src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^5.8.0"
  }
}
